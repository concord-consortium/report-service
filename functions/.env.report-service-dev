AWS_S3_BUCKET=concord-staging-report-data
OPENAI_MODEL=gpt-5.5
AUTHORING_HOST=authoring.lara.staging.concord.org
TRUSTED_PORTAL_HOSTS=learn.portal.staging.concord.org

# Researcher Dashboard launch surface (run_package). All four are outputs of the
# researcher-dashboard-runner-staging stack in 612297603577 and none is secret.
RD_MICROVM_IMAGE_ARN=arn:aws:lambda:us-east-1:612297603577:microvm-image:researcher-dashboard-runner-staging
RD_EXECUTION_ROLE_ARN=arn:aws:iam::612297603577:role/researcher-dashboard-runner-staging-execution
RD_DATA_BUCKET=researcher-dashboard-runner-staging
RD_REPORT_SERVER_URL=https://report-server.concordqa.org

# RIGSE-365 spike only: attaches SHELL_INGRESS to each launched MicroVM, which yields an
# interactive root PTY on port 8022 to anyone who can call create-microvm-shell-auth-token.
# It is here to exercise the runner-died recovery path against a real VM. Production must
# not set it, and this line should go when the spike's stories are written.
RD_SHELL_INGRESS=1
